{
  "name": "webgpu-image-filter",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode demo",
    "build:demo": "vite build --mode demo",
    "build:lib": "vite build --mode lib",
    "release": "pnpm build:lib && pnpm publish",
    "deploy": "sh deploy.sh"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.0",
    "@tweakpane/core": "^1.1.1",
    "@webgpu/types": "^0.1.43",
    "lru_map": "^0.4.1",
    "tweakpane": "^3.1.1",
    "typescript": "^5.5.3",
    "vite": "^4.0.3",
    "vite-plugin-glsl": "1.0.1"
  }
}
